# Mastermind-Numbers 🎯

A classic number guessing game built in C language where the player tries to guess the secret number.

## 🚀 How to Run
1. Download or clone `main.c`
2. Compile and run using GCC:
   ```powershell
   gcc main.c -o game
   .\game.exe
   ```